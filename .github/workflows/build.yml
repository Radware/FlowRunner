name: FlowRunner Build

on:
  push:
    branches: [ "main", "master" ]

jobs:
  build:
    runs-on: ${{ matrix.os }}

    strategy:
      matrix:
        os: [macos-latest, windows-latest, ubuntu-latest]

    steps:
      - name: Check out git repository
        uses: actions/checkout@v4 # Also a good practice to update this to v4

      - name: Set up Node.js
        uses: actions/setup-node@v4 # And this one too
        with:
          node-version: '18'
          cache: 'npm'

      - name: Install dependencies
        run: npm install

      - name: Build application
        run: npm run dist
        
      - name: Upload Artifacts
        uses: actions/upload-artifact@v4 # The required fix is here
        with:
          name: ${{ matrix.os }}-build
          path: dist/